<manifest package="com.longshihan.arm"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <!-- glide配置 ,清单文件合并会合并在一起-->
        <meta-data
            android:name="com.longshihan.arm.strategy.imageloader.glide.GlideConfiguration"
            android:value="GlideModule"/>
    </application>
</manifest>
